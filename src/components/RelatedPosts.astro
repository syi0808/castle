---
import type { BlogPost } from "../lib/posts";

interface Props {
  posts: BlogPost[];
}

const { posts } = Astro.props;
---

{posts.length > 0 && (
  <section class="mt-16 pt-6 border-t border-(--color-border)">
    <h2 class="text-lg font-medium tracking-tight mb-4">Related</h2>
    <ul class="space-y-1.5">
      {posts.map((post) => (
        <li>
          <a
            href={`${import.meta.env.BASE_URL}${post.id}`}
            class="text-base text-(--color-text-sub) hover:text-(--color-text) transition-colors"
          >
            {post.data.title}
          </a>
        </li>
      ))}
    </ul>
  </section>
)}
