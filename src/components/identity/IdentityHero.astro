<section class="identity-hero site-shell">
  <p class="eyebrow">Software engineer · Seoul</p>
  <h1>Yein Sung</h1>
  <p class="introduction">I make tools, interfaces, and things I wish existed.</p>
  <p class="details">Currently exploring open-source developer tools and thoughtful interactions for the web.</p>
</section>

<style>
  .identity-hero { padding: clamp(5rem, 11vw, 9rem) 0 clamp(6rem, 12vw, 10rem); border-bottom: 1px solid var(--color-border); }
  .eyebrow { margin: 0 0 clamp(2rem, 5vw, 4rem); color: var(--color-text-sub); font: .67rem var(--font-mono); text-transform: uppercase; letter-spacing: .08em; }
  h1 { margin: 0; font-size: clamp(4.5rem, 11vw, 9rem); font-weight: 500; letter-spacing: -.085em; line-height: .82; }
  .introduction { max-width: 52rem; margin: clamp(2.5rem, 6vw, 4.5rem) 0 0; font-size: clamp(1.7rem, 3.8vw, 3.25rem); font-weight: 500; letter-spacing: -.05em; line-height: 1.08; }
  .details { max-width: 34rem; margin: 2rem 0 0; color: var(--color-text-sub); font-size: .92rem; line-height: 1.7; }

  @media (max-width: 700px) {
    .identity-hero { padding-top: 3.5rem; }
    h1 { font-size: clamp(4.2rem, 21vw, 6rem); }
    .introduction { margin-top: 2.5rem; font-size: clamp(1.6rem, 7.5vw, 2.25rem); }
  }
</style>
