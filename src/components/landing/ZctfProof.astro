<div class="zctf-proof" data-proof="3" aria-hidden="true">
  <div class="scope">1,000,000 packages</div>
  <div class="memory" role="img" aria-label="Little-endian binary document split into header, root, list region, string table, and string heap">
    <div class="segment header"><span>header</span><b>64 bytes</b></div>
    <div class="segment root"><span>root</span></div>
    <div class="segment list"><span>list region</span></div>
    <div class="segment table"><span>string table</span></div>
    <div class="segment heap"><span>string heap</span></div>
  </div>
  <div class="evidence">
    <div class="fast"><strong>0.020ms</strong><span>view · first access</span></div>
    <div><strong>162.866ms</strong><span>JSON parse + first access</span></div>
  </div>
</div>

<style>
  .zctf-proof { display: grid; align-content: center; width: min(82vw, 70rem); min-height: min(48vh, 29rem); }
  .scope { margin-bottom: 1.2rem; color: color-mix(in srgb, var(--stage-ink) 50%, transparent); font: .68rem var(--font-mono); }
  .memory { display: grid; grid-template-columns: 18fr 16fr 22fr 18fr 26fr; min-height: clamp(8rem, 15vw, 12rem); border: 1px solid color-mix(in srgb, var(--stage-ink) 52%, transparent); }
  .segment { display: flex; flex-direction: column; justify-content: space-between; padding: clamp(.7rem, 1.5vw, 1.25rem); border-right: 1px solid color-mix(in srgb, var(--stage-ink) 28%, transparent); }
  .segment:last-child { border-right: 0; }
  .segment span { color: color-mix(in srgb, var(--stage-ink) 72%, transparent); font-size: clamp(.72rem, 1.2vw, .95rem); }
  .segment b { color: color-mix(in srgb, var(--stage-ink) 42%, transparent); font: 400 .62rem var(--font-mono); }
  .table { position: relative; color: var(--color-accent); background: color-mix(in srgb, var(--color-accent) 18%, transparent); box-shadow: inset 0 -3px 0 var(--color-accent); }
  .table span { color: var(--stage-ink); }
  .evidence { display: grid; grid-template-columns: 1fr 1fr; gap: 2rem; margin-top: 2rem; }
  .evidence > div { display: grid; }
  .evidence strong { color: color-mix(in srgb, var(--stage-ink) 72%, transparent); font: 500 clamp(1.3rem, 2.5vw, 2.2rem) var(--font-mono); letter-spacing: -.04em; }
  .evidence span { color: color-mix(in srgb, var(--stage-ink) 42%, transparent); font: .64rem var(--font-mono); }
  .evidence .fast strong { color: var(--color-accent); }

  @media (max-width: 700px) {
    .zctf-proof { width: calc(100vw - 2rem); }
    .memory { grid-template-columns: 1fr; min-height: 0; }
    .segment { flex-direction: row; min-height: 3.2rem; border-right: 0; border-bottom: 1px solid color-mix(in srgb, var(--stage-ink) 25%, transparent); }
    .segment:last-child { border-bottom: 0; }
    .table { box-shadow: inset 3px 0 0 var(--color-accent); }
    .evidence { gap: 1rem; }
    .evidence strong { font-size: clamp(1rem, 5vw, 1.4rem); }
  }
</style>
